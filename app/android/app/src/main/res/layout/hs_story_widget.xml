<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/hs_story_widget_background"
    android:padding="14dp">

    <!-- 標頭：站名 + 位置 i/N -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="聖靈故事"
            android:textSize="13sp"
            android:letterSpacing="0.05"
            android:textColor="#8A8578" />

        <TextView
            android:id="@+id/widget_pos"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="13sp"
            android:textColor="#8A8578"
            android:text="" />
    </LinearLayout>

    <TextView
        android:id="@+id/widget_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:maxLines="2"
        android:ellipsize="end"
        android:textStyle="bold"
        android:textSize="20sp"
        android:textColor="#2B2620"
        android:text="最新聖靈故事" />

    <!-- 內文：顯示全文（widget 可放大看更多；很長的故事點標題開全文/用朗讀） -->
    <TextView
        android:id="@+id/widget_body"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="8dp"
        android:maxLines="40"
        android:ellipsize="end"
        android:lineSpacingExtra="2dp"
        android:textSize="15sp"
        android:textColor="#3A362E"
        android:text="開啟 App 載入最新故事" />

    <!-- 控制列：◀　🔊 朗讀　▶ -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:orientation="horizontal"
        android:gravity="center">

        <TextView
            android:id="@+id/widget_prev"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:minWidth="54dp"
            android:paddingTop="9dp"
            android:paddingBottom="9dp"
            android:gravity="center"
            android:background="@drawable/widget_btn"
            android:text="◀"
            android:textSize="17sp"
            android:textColor="#2B2620" />

        <TextView
            android:id="@+id/widget_speak"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="8dp"
            android:paddingTop="9dp"
            android:paddingBottom="9dp"
            android:gravity="center"
            android:background="@drawable/widget_btn_accent"
            android:text="🔊 朗讀"
            android:textStyle="bold"
            android:textSize="15sp"
            android:textColor="#FFFFFF" />

        <TextView
            android:id="@+id/widget_next"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:minWidth="54dp"
            android:paddingTop="9dp"
            android:paddingBottom="9dp"
            android:gravity="center"
            android:background="@drawable/widget_btn"
            android:text="▶"
            android:textSize="17sp"
            android:textColor="#2B2620" />
    </LinearLayout>

</LinearLayout>
